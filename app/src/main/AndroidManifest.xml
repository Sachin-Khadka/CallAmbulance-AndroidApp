<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.joker.callambulance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".landing.BloodDonor"></activity>
        <activity android:name=".landing.AmbulanceServices" />
        <activity android:name=".DetailedActivity" />
        <activity android:name=".landing.HospitalInfo" />
        <activity android:name=".landing.EmergencyLanding" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".HomePage" />
        <activity android:name=".UserProfile" />
        <activity android:name=".Register" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>